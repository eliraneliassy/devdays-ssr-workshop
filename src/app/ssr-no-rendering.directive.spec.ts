import { SsrNoRenderingDirective } from './ssr-no-rendering.directive';

describe('SsrNoRenderingDirective', () => {
  it('should create an instance', () => {
    const directive = new SsrNoRenderingDirective();
    expect(directive).toBeTruthy();
  });
});
