import { SsrRenderingDirective } from './ssr-rendering.directive';

describe('SsrRenderingDirective', () => {
  it('should create an instance', () => {
    const directive = new SsrRenderingDirective();
    expect(directive).toBeTruthy();
  });
});
